# Server Configuration
PORT=3000
NODE_ENV=development # Set to 'production' in production environment

# Database Configuration
DB_HOST=localhost
DB_USER=mysql
DB_PASSWORD=password
DB_NAME=producto-db
DB_PORT=3306

# CORS Configuration
FRONTEND_URL=http://localhost:19006 # Set to your production frontend URL in production

# API URL Configuration
API_URL=http://localhost:3000 # Set to your production API URL in production

# File Upload Configuration
MAX_FILE_SIZE=10485760 # 10MB in bytes

# Optional: Cloudinary Configuration (for production image hosting)
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret
